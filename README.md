# push_swap

push_swap is a small sorting project, takes a list of integers and prints the operations needed to sort them using two stacks

## Build

Run:

make

## Use

give the numbers as arguments:

./push_swap 3 2 1
the program will print the list of moves to sort the values

## Clean

Run:

make fclean